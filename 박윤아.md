<인프런 강의내용 정리>

#1 기존의 단점 : 파일이 많아 지저분하며, 각 파일에 대한 변경 기록이 없음
               -> 상쇄하는게 'git'

#2 git : 코드 변경점 기록 / 소프트웨어의 변경 사항을 체계적으로 추적하고 통제
         -> 링크로 팀원에게 쉽게 공유 및 협업 가능 (온라인 코드 저장소)

#3 리눅스 명령어 
-pwd : 현재 내가 작업하고 있는 폴더의 위치를 보여줌
-ls : 현재 폴더 안에 있는 파일과 폴더를 보여줌
-ls -a : 숨겨진 파일까지 포함해서 모든 파일과 폴더를 보여줌
-cd : 원하는 폴더로 이동할 때 사용함


#4 git 명령어 
1.git init : 현재 폴더를 Git으로 관리하기 위한 초기 설정을 함
2.git add : 저장할 파일을 지정함 / git add 파일명
3.git commit : 변경한 내용을 Git에 저장함 / git commit -m "메시지"
4.git status : 현재 파일의 변경 상태를 확인함
5.git log : 지금까지의 commit 저장 내역을 확인함
6.git branch : 브랜치를 확인하거나 새로운 브랜치를 만듦 / git branch 브랜치이름
7.git switch, git checkout : 다른 브랜치로 이동함 / git switch 브랜치이름, git checkout 브랜치이름
8.git merge : 8. git merge : 다른 브랜치의 내용을 현재 브랜치에 합침 / git merge 브랜치이름
9.git reset : 과거의 기록으로 돌아감
10.git stash : 현재 작업 중인 코드를 임시로 저장함
11.git revert : 과거 commit의 변경 내용을 되돌리는 새로운 commit을 만듦

*작업 순서
branch -> 작업 -> add -> commit -> push

<노션 내용 정리>

#1 버전 관리 시스템(VCS)
: 코드가 바뀔 때마다 스냅샷(Snapshot)을 남겨서, 누가 무엇을 왜 바꿨는지 추적하고 필요하면 이전 상태로 되돌릴 수 있게 해줌

#2 로컬 저장소(Local Repository)
: 내 컴퓨터 안에 있는, 커밋 기록이 저장된 폴더

#3 원격 저장소(Remote Repository)
: GitHub 서버에 있는, 팀원 모두가 공유하는 저장소

#4 이슈(Issue)
: 할 일이나 버그를 기록하고 담당자를 지정하는 GitHub 기능
누가 무엇을 하고 있는지 팀 전체가 파악할 수 있게 해줌

#5 풀 리퀘스트(PR - Pull Request)
: 브랜치의 변경사항을 main에 합쳐도 될지 검토해달라는 요청
바로 합치지 않고 PR을 거치는 이유는, 합치기 전에 팀원이 한 번 검토(코드 리뷰)해서 실수를 미리 걸러내기 위함

#6 코드 리뷰(Code Review)
: 팀원의 PR을 읽고 코멘트를 남기는 것